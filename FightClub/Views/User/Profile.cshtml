@model UserProfile

@{
    Layout = "_Layout";
    ViewBag.Title = "Profile";
}

<div>
    <div>Name: @Model.UserName</div>
    <div>Id: @Model.Id</div>
    @if (Model.ClubId != null)
    {
        <div>ClubId: @Model.ClubId</div>
    }
</div>
